import React, { useContext } from 'react';
var initialState = {
  onChange: function onChange() {
    return {};
  },
  activeItem: ''
};
export var TabsContext = /*#__PURE__*/React.createContext(initialState);
export default (function () {
  return useContext(TabsContext);
});