import React, { useContext } from 'react';
var initialState = {
  onToggle: function onToggle() {
    return;
  },
  activeItem: '',
  variant: 'default'
};
export var AccordionContext = /*#__PURE__*/React.createContext(initialState);
export default (function () {
  return useContext(AccordionContext);
});