import React from 'react';
import SlideDown from '../../Utility/SlideDown';
import { useNavLocation } from './NavLocationContext.internal';

var Expander = function Expander(_ref) {
  var children = _ref.children,
      expanded = _ref.expanded,
      autoScroll = _ref.autoScroll;

  var _useNavLocation = useNavLocation(),
      where = _useNavLocation.where,
      open = _useNavLocation.open;

  return /*#__PURE__*/React.createElement(SlideDown, {
    contentClassName: "bf-nav-group-content",
    open: !!open && (where === 'side' && !open ? false : expanded),
    autoScroll: autoScroll
  }, children);
};

export default Expander;