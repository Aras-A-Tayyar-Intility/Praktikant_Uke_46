import React, { createContext, useContext } from 'react';
var NavLocationContext = /*#__PURE__*/createContext({});
NavLocationContext.displayName = 'NavLocationContext';

var useNavLocation = function useNavLocation() {
  return useContext(NavLocationContext);
};

var NavLocationProvider = function NavLocationProvider(_ref) {
  var where = _ref.where,
      open = _ref.open,
      children = _ref.children;
  return /*#__PURE__*/React.createElement(NavLocationContext.Provider, {
    value: {
      where: where,
      open: open
    }
  }, children);
};

export { useNavLocation };
export default NavLocationProvider;