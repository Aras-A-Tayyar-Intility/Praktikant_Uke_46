import React from 'react';
import PropTypes from 'prop-types';
import useBreakpoint from './useBreakpoint';
var breakpointValues = ['small', 'medium', 'large', 'xl', 'xxl'];
export default function Breakpoint(_ref) {
  var children = _ref.children,
      from = _ref.from,
      to = _ref.to;
  var match = useBreakpoint(from !== null && from !== void 0 ? from : null, to);
  return /*#__PURE__*/React.createElement(React.Fragment, null, match ? children : null);
}
Breakpoint.propTypes = {
  children: PropTypes.node,
  from: PropTypes.oneOf(breakpointValues),
  to: PropTypes.oneOf(breakpointValues)
};