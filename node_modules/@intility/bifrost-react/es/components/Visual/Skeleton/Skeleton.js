import React from 'react';
import PropTypes from 'prop-types';
import SkeletonRect from './Skeleton.Rect';
import SkeletonCardImage from './Skeleton.CardImage';
import SkeletonText from './Skeleton.Text';

/** Wrapper for repeating elements */
var Skeleton = function Skeleton(_ref) {
  var children = _ref.children,
      repeat = _ref.repeat;
  return /*#__PURE__*/React.createElement(React.Fragment, null, Array(repeat).fill(children));
};

Skeleton.propTypes = {
  /** Skeleton content components */
  children: PropTypes.node,

  /** Number of how many times children should be rendered/repeated */
  repeat: PropTypes.number
};
Skeleton.defaultProps = {
  repeat: 1
};
Skeleton.Rect = SkeletonRect;
Skeleton.CardImage = SkeletonCardImage;
Skeleton.Text = SkeletonText;
Skeleton.displayName = 'Skeleton';
export default Skeleton;