// this module is imported globally for bifrost
import { hideAll } from 'tippy.js'; // don't assume window is available, in case someone makes a server-side rendered page

if (typeof window !== 'undefined') {
  var _window, _window$document, _window$document$body;

  (_window = window) === null || _window === void 0 ? void 0 : (_window$document = _window.document) === null || _window$document === void 0 ? void 0 : (_window$document$body = _window$document.body) === null || _window$document$body === void 0 ? void 0 : _window$document$body.addEventListener('keydown', function (e) {
    if (!e.repeat && e.key === 'Escape') hideAll();
  });
}