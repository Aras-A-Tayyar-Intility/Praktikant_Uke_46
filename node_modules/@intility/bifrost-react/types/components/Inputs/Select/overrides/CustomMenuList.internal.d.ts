/// <reference types="react" />
import PropTypes from 'prop-types';
declare const CustomMenuList: {
    (props: any): JSX.Element;
    propTypes: {
        children: PropTypes.Requireable<object>;
    };
};
export default CustomMenuList;
