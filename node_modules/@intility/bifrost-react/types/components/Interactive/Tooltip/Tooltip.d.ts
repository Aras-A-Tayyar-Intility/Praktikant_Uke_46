import React from 'react';
import { TippyProps } from '@tippyjs/react';
import { State } from '../../../types';
export declare type TooltipProps = Omit<TippyProps, 'ref'> & {
    state?: State;
};
declare const Tooltip: React.FC<TooltipProps>;
export default Tooltip;
