import { RefObject } from 'react';
declare function useOverflow(ref: RefObject<HTMLElement>): {
    refOverflow: boolean;
    refScrollBegin: boolean;
    refScrollEnd: boolean;
};
export default useOverflow;
