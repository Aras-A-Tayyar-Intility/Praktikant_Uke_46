import React from 'react';
import ReactModal from 'react-modal';
import { IconProp } from '@fortawesome/fontawesome-svg-core';
import { ModalFooterProps } from './Modal.Footer';
import { ForwardRefType } from '../../../types';
export declare type ModalProps = React.PropsWithChildren<ReactModal.Props & {
    header: string;
    icon?: IconProp;
    headerChildren?: React.ReactNode;
    noPadding?: boolean;
    noHeader?: boolean;
}>;
export declare type ModalType = React.FC<ModalProps> & {
    setAppElement: (appElement: string | HTMLElement) => void;
    Footer: ForwardRefType<HTMLElement, ModalFooterProps>;
};
declare const Modal: ModalType;
export default Modal;
