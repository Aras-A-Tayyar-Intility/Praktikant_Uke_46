/// <reference types="react" />
import PropTypes from 'prop-types';
export declare type FormatDateProps = {
    date: Date;
    show?: 'date' | 'datetime' | 'time';
};
declare const FormatDate: {
    ({ date, show }: FormatDateProps): JSX.Element;
    defaultProps: {
        show: string;
    };
    propTypes: {
        date: PropTypes.Requireable<Date>;
        show: PropTypes.Requireable<string>;
    };
};
export default FormatDate;
